# nestjs-unit-tests

NestJS unit tests with Jest — co-location, mocks, services, controllers.

**What it does:** *.spec.ts; Test.createTestingModule; useValue, jest.fn(); Services, Controllers, Guards, Pipes.

## Quick install

```bash
git clone https://github.com/mariocosttaa/my-agent-skills.git && cp -r my-agent-skills/nestjs-unit-tests ~/.cursor/skills/
```

→ [See all skills](https://github.com/mariocosttaa/my-agent-skills)
